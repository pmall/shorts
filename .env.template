# Reddit API Credentials
# Get these from https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=your_script_name/1.0.0 by /u/yourusername

# Database Configuration
# Use 'sqlite' or 'postgresql'
DB_TYPE=sqlite

# SQLite Configuration (if DB_TYPE=sqlite)
DB_PATH=./stories.db

# PostgreSQL Configuration (if DB_TYPE=postgresql)
DB_STRING=postgresql://user:password@localhost:5432/database